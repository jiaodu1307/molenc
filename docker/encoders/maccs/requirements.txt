# MACCS encoder requirements
# Base requirements are already included in the base Docker image
# Only add encoder-specific dependencies here

# FastAPI for web service
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0